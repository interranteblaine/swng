// Placeholder shell — this task only scaffolds the app and the React<->SDK seam. Routes for
// home, create, join-by-code, and the round view land on top of this in later M5 work
// (docs/implementation-plan.md's M5 task list).
export function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 text-slate-100">
      <p>swng</p>
    </main>
  );
}
